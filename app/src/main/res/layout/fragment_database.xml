<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Database"
    android:background="#ffffff"
    android:tag="database"
    >
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >

        <Button
            android:id="@+id/button"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:background="@drawable/no_background_button"
            android:drawableStart="@drawable/idreal"
            android:text="Your IDs"
            android:textAllCaps="false"
            android:textColor="@color/colorSaiNote"
            android:textColorHighlight="@color/lightBlueColor"
            android:textSize="20sp" />

        <ScrollView
            android:layout_below="@+id/button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/linearLayout"
                android:orientation="vertical">
            </LinearLayout>
        </ScrollView>

    </RelativeLayout>

</FrameLayout>